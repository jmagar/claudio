# Redis Configuration (Optional - for message caching)

# Option 1: Local Redis (preferred if you have Redis running locally)
REDIS_URL=redis://localhost:6379
# OR use separate host/port
# REDIS_HOST=localhost
# REDIS_PORT=6379

# Option 2: Upstash Redis (serverless cloud option)
# UPSTASH_REDIS_REST_URL=your_redis_url_here
# UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# Setup Instructions:
# 1. For local Redis: Install Redis locally (docker run -p 6379:6379 redis)
# 2. For Upstash: Sign up at https://upstash.com and get credentials
# 3. Rename this file to .env.local and uncomment your preferred option

# Note: The app works fine without Redis - it will just use localStorage only